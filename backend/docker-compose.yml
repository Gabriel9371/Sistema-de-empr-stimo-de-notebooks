services:
  postgres:
    image: postgres:16-alpine
    container_name: notebooks-db
    environment:
      POSTGRES_DB: notebooks
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"
    volumes:
      - notebooks_data:/var/lib/postgresql/data
volumes:
  notebooks_data:
